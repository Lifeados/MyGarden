name: my_garden
description: "Projeto de análise de plantas."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.1

dependencies:
  smooth_page_indicator: 1.2.0+3
  flutter:
    sdk: flutter
    
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.10+1
  camera: ^0.11.0+2
  firebase_core: ^3.9.0
  firebase_auth: ^5.3.4
  google_sign_in: ^6.2.2
  flutter_facebook_auth: ^7.1.1
  provider: ^6.1.2
  cloud_firestore: ^5.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/ui/assets/images/
    - lib/ui/assets/icons/
